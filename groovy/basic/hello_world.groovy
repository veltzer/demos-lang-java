#!/usr/bin/groovy

// A basic hello world example in groovy.
// As you can see, line starting with // are comments.

/*
	notice that:
	- groovy source files are usually called .groovy
	- you don't HAVE to write OO code in groovy (this is similar to
	other scripting languages like Perl, Python and more).
	- this makes it easy for sys admins and programmers to use groovy
	for small tasks.
*/

/*
	This is
	a
	multi
	line
	comment
*/

println "Hello, World!" // this is a comment that spans till the end of the line
