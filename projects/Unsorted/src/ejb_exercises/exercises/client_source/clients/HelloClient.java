package ejb_exercises.exercises.client_source.clients;

/**
 * A test client for our Greet bean.
 *
 * This simple test obtains a Greet stub and invokes its method(s).
 */
public class HelloClient {
	public static void main(String[] args) {
		//create an InitialContext
		//lookup a remote object
		//cast the remote object into the correct home interface
		//use the home interface to create an object interface
		//use the object interface to invoke the bean's methods
		//use the object interface to notify the application
		//server to release the bean back to the pool
	}
}
